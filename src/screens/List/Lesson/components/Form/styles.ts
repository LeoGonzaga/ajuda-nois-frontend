import styled from 'styled-components';

const Container = styled.div`
  max-height: 90vh;

  margin: 10px;
  input {
    width: 100%;
  }

  img {
    max-width: 1000px;
  }
`;

export const Styles = {
  Container,
};
